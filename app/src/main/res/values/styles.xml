<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="SpinnerButtonStyle" parent="android:Widget.Holo.Spinner">
        <item name="android:textColor">@color/preto</item>
        <item name="android:textAppearance">?android:attr/textAppearanceMedium</item>
    </style>

    <style name="ButtonConfirm" parent="Base.Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/button_color_blue</item>
    </style>

    <style name="ButtonCancel" parent="Base.Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/button_color_cancel</item>
    </style>

    <style name="ButtonIcon" parent="Base.Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/branco</item>
    </style>

    <style name="MyTheme" parent="MyTheme.Base">
    </style>
    <!-- Base theme applied no matter what API -->
    <style name="MyTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--If you are using revision 22.1 please use just windowNoTitle. Without android:-->
        <item name="windowNoTitle">true</item>
        <!--We will be using the toolbar so no need to show ActionBar-->
        <item name="windowActionBar">false</item>
        <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette-->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/cor_fundo</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/cor_fundo</item>
        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">@color/accent</item>
        <item name="android:progressBarStyle">@color/cor_forte</item>
        <!-- You can also set colorControlNormal, colorControlActivated
            colorControlHighlight and colorSwitchThumbNormal. -->
    </style>
</resources>
